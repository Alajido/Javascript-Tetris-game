// get the whole container of our grid
const grid = document.querySelector(`.grid`);
// get all the child div from our grid container and pass them as an array
const squares = document.querySelectorAll(`.grid div`);